import { BeStickyDirective } from './be-sticky.directive';

describe('BeStickyDirective', () => {
  it('should create an instance', () => {
    const directive = new BeStickyDirective();
    expect(directive).toBeTruthy();
  });
});
